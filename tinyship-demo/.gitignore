# File purpose: Ignore local development files while keeping demo env files committed.

node_modules/

# The demo commits non-secret production fixtures and templates so TinyShip
# can validate a complete multi-service deployment example from Git.
.env
.env.*
apps/*/.env.*
!apps/*/.env.example
!apps/*/.env.production
