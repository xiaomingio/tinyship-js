{
  "name": "@tinyship-demo/user",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/user.ts --bundle --platform=node --format=esm --target=node20 --sourcemap --outfile=dist/user.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.1"
  }
}
